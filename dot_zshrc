# path updates
path+=("${HOME}/.local/bin")

# mise tools
eval "$(mise activate)"
mise install --quiet

# prompt
eval "$(starship init zsh)"

# bun
[ -s "/Users/jbassin/.bun/_bun" ] && source "/Users/jbassin/.bun/_bun"
